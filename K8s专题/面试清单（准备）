
# K8s 基础设施开发面试题清单（附参考答案）

## 适用岗位画像

基于你提供的 JD，这个岗位重点不是“会用 K8s”这么简单，而是更偏：

- **分布式调度编排**
- **训练/推理任务部署**
- **集群调度策略优化**
- **CPU/GPU/异构资源联合分配**
- **多集群/多地域/多云适配**
- **抢占/驱逐/弹性扩缩容**
- **训练稳定性、Failover、可观测性**
- **Linux + Go/Python 工程能力**

所以面试题通常会围绕下面 6 类展开：

1. Kubernetes 基础原理  
2. 调度与资源管理  
3. 机器学习训练场景与异构资源  
4. 高可用、稳定性、可观测性  
5. Linux / Go / Python / 分布式基础  
6. 项目设计与排障能力  

---

## 一、Kubernetes 基础原理

### 1. 什么是 Pod？为什么 K8s 不直接调度容器？
**参考答案：**

Pod 是 Kubernetes 中最小的调度单元，一个 Pod 里可以包含一个或多个共享网络、存储、命名空间的容器。  
K8s 不直接调度容器，是因为很多业务组件天然需要“协同运行”，比如主容器和日志 sidecar、代理 sidecar。Pod 抽象更适合描述这类强耦合单元。

---

### 2. Pod 创建的完整流程是什么？
**参考答案：**

可以按这个链路回答：

1. 用户通过 `kubectl` 或 API 提交 Pod/Deployment YAML 到 `kube-apiserver`
2. 对象被写入 `etcd`
3. `scheduler` 监听到未绑定节点的 Pod，进行筛选和打分，决定调度到哪个 Node
4. 调度结果写回 API Server
5. 目标节点上的 `kubelet` 监听到 Pod 变更，调用容器运行时（如 `containerd`）拉镜像、创建容器
6. 同时配置网络插件 CNI、挂载卷
7. 容器启动后汇报状态，Pod 进入 Running 等阶段

面试里最好补一句：  
**控制面负责“声明式期望状态”，节点侧负责把期望状态落地。**

---

### 3. Deployment、StatefulSet、DaemonSet 的区别是什么？
**参考答案：**

- **Deployment**：适合无状态服务，支持滚动更新、回滚、弹性扩缩
- **StatefulSet**：适合有状态应用，Pod 名称稳定、PVC 稳定、启动停止有序
- **DaemonSet**：保证每个节点上运行一个 Pod，适合日志采集、监控 Agent、网络插件

如果岗位偏基础设施，建议再补一句：  
`DaemonSet` 常用于部署节点级基础组件，`StatefulSet` 常用于数据库、消息队列等。

---

### 4. Service 是什么？它为什么能实现服务发现和负载均衡？
**参考答案：**

Service 是一组 Pod 的稳定访问入口，通过 Label Selector 选中后端 Pod。  
其核心作用：

- 提供稳定虚拟 IP（ClusterIP）
- 让客户端不需要感知 Pod IP 变化
- 借助 `kube-proxy` 通过 iptables/ipvs 规则转发到后端 Pod

本质上，Service 自己不是代理进程，而是 **规则 + 虚拟 IP + Endpoint 管理机制**。

---

### 5. kube-proxy 的 iptables 和 ipvs 有什么区别？
**参考答案：**

- **iptables**：基于规则匹配，简单直接，规模变大时规则链较长，性能和可维护性可能下降
- **ipvs**：基于内核态负载均衡，支持更多调度算法，性能更好，适合大规模集群

一般可以回答：  
**中小规模集群 iptables 够用；大规模 Service 较多时 ipvs 更有优势。**

---

### 6. etcd 为什么重要？
**参考答案：**

etcd 是 K8s 的一致性存储，保存了集群的元数据和期望状态。  
如果 etcd 不可用，控制面虽然部分组件可能仍在运行，但整个集群的状态读写会受影响。  
它之所以可靠，是因为底层使用了 **Raft 一致性协议** 来保证强一致。

---

### 7. K8s 中声明式 API 的核心思想是什么？
**参考答案：**

用户只描述“我要什么”，而不是“怎么做”。  
比如声明副本数是 3，控制器会持续对比“当前状态”和“期望状态”，通过 Reconcile 让系统收敛。  
这使得系统具备自动恢复、自愈、持续校正能力。

---

## 二、调度与资源管理

### 8. K8s 调度器的基本工作流程是什么？
**参考答案：**

调度器通常分两步：

1. **过滤（Filter）**：筛掉不满足条件的节点，比如资源不足、污点不容忍、亲和性不满足
2. **打分（Score）**：对剩余节点评分，比如资源均衡、节点负载、亲和性偏好

最后选出分数最高的节点进行绑定。

---

### 9. 影响调度结果的常见因素有哪些？
**参考答案：**

常见包括：

- CPU / 内存 / GPU 资源是否足够
- `nodeSelector` / `nodeAffinity`
- `podAffinity` / `podAntiAffinity`
- `taint` / `toleration`
- PVC 能否绑定
- topology spread constraints
- 优先级和抢占策略

如果是字节这个 JD，建议强调：  
**不仅是资源够不够，还包括拓扑、优先级、业务类型、混部策略。**

---

### 10. 什么是 requests 和 limits？两者有什么区别？
**参考答案：**

- `requests`：调度依据，表示 Pod 至少需要多少资源
- `limits`：运行上限，限制 Pod 最多可使用多少资源

调度器主要看 `requests`，不是看实时使用量。  
CPU 超限通常是限速，内存超限可能导致 OOMKilled。

---

### 11. Pod 一直处于 Pending，你怎么排查？
**参考答案：**

优先看：

1. `kubectl describe pod` 看 Events
2. 看是不是资源不足
3. 看亲和性、污点、nodeSelector 是否冲突
4. 看 PVC 是否未绑定
5. 看是否有 GPU、拓扑等特殊资源约束
6. 看调度器日志

面试中推荐一句话模板：  
**先看事件，再区分是资源问题、约束问题，还是存储/特殊设备问题。**

---

### 12. 什么是优先级和抢占（Preemption）？
**参考答案：**

当高优先级 Pod 无法调度时，调度器可以驱逐部分低优先级 Pod，腾出资源给高优先级 Pod。  
适用于在线服务、核心训练任务等更重要负载。  
但要注意：

- 抢占会影响低优先级业务稳定性
- 不能滥用
- 需要和业务 SLA、资源池隔离一起设计

---

### 13. 驱逐（Eviction）和删除 Pod 有什么不同？
**参考答案：**

Eviction 是 K8s 受控的驱逐机制，会考虑：

- PDB（PodDisruptionBudget）
- 优雅终止时间
- 节点压力状态

而直接删除 Pod 更像强制动作，不一定会考虑这些约束。  
基础设施岗位通常更关注**受控中断**，所以 Eviction 更重要。

---

### 14. HPA、VPA、Cluster Autoscaler 的区别是什么？
**参考答案：**

- **HPA**：横向扩容，增加或减少 Pod 副本数
- **VPA**：纵向扩容，调整 Pod 的资源请求
- **Cluster Autoscaler**：节点层扩缩容，节点不够就扩节点，节点空闲就缩节点

在训练/推理场景里，HPA 常用于服务副本扩缩；  
节点扩容更适合应对 GPU 节点不足。

---

### 15. 多租户场景下，资源治理通常怎么做？
**参考答案：**

常见手段：

- Namespace 隔离
- ResourceQuota / LimitRange
- PriorityClass
- 节点池隔离
- NetworkPolicy
- 成本和预算约束
- 配额、超卖、保障资源与非保障资源分层

如果结合 JD，可以答：  
**多租户不仅是资源隔离，还要考虑预算、交付、混部、超卖策略。**

---

## 三、机器学习训练 / GPU / 异构资源

### 16. 为什么机器学习训练任务的调度比普通 Web 服务更复杂？
**参考答案：**

因为训练任务往往具有这些特点：

- 需要 GPU/NPU 等异构资源
- 多机多卡通信，对网络拓扑敏感
- 分布式任务要求多个副本同时就绪
- 训练时间长，对中断敏感
- 数据、模型、算力、网络要联合匹配
- 容易出现碎片化和资源浪费

所以不能只做“够不够调度”，还要做“调得好不好”。

---

### 17. GPU 调度和 CPU 调度的差异是什么？
**参考答案：**

GPU 调度更关注：

- 设备数量和类型
- 卡间拓扑，比如 NVLink、PCIe、同机/跨机
- 任务对显存、带宽的敏感性
- 碎片化问题
- 训练任务 gang scheduling 需求

CPU 更偏通用资源；  
GPU 更像稀缺且强约束资源。

---

### 18. 什么是 Gang Scheduling？为什么训练场景经常需要它？
**参考答案：**

Gang Scheduling 指一个任务组里的多个 Pod 必须“整体满足”后一起调度，否则宁可都不启动。  
因为分布式训练通常要求 worker、ps、master 等角色协同启动，如果只启动一部分，会浪费资源甚至卡住。  
这和普通无状态服务“一部分先启动也能工作”不同。

---

### 19. 训练任务中常见的资源碎片化问题是什么？怎么优化？
**参考答案：**

比如集群里剩很多零散 GPU，但没有足够连续/同拓扑资源给一个大训练任务使用，这就是碎片化。  
优化思路包括：

- 优先按任务规格做资源池规划
- 引入 binpacking 或反碎片化调度策略
- 任务分级，长任务与短任务分池
- 做预约、抢占、回填
- 考虑拓扑感知调度

---

### 20. 什么是拓扑感知调度？
**参考答案：**

调度时不仅看资源量，还看资源的位置关系，比如：

- 同机优于跨机
- 同交换机优于跨交换机
- 同可用区优于跨可用区
- GPU 之间的互联结构

其目标是减少通信成本、提升训练效率、降低网络拥塞。  
这和 JD 里提到的“微拓扑优化、优化整体网络带宽使用”高度一致。

---

### 21. 如果一个训练任务既依赖 GPU，又依赖大数据集和网络带宽，你会怎么设计调度策略？
**参考答案：**

可以从“联合约束”回答：

1. 先判断 GPU 类型和数量是否满足
2. 再考虑数据 locality 或缓存位置
3. 再考虑网络拓扑和带宽
4. 最后按业务优先级、时效性做排序

核心思想：  
**不要只按单资源维度调度，要做 CPU/GPU/网络/数据的联合匹配。**

---

### 22. 多副本训练中，如果某个 worker 异常退出，如何提升稳定性？
**参考答案：**

可以从这些方面说：

- 自动重试和故障恢复
- checkpoint / savepoint
- failover 策略
- 区分可恢复错误和不可恢复错误
- 副本重建时尽量复用已有状态
- 完善监控与告警

如果面试官继续追问，可以补：  
**重点是降低单点失败导致整任务失败的概率。**

---

## 四、多集群 / 高可用 / 稳定性 / 可观测性

### 23. 多集群调度的核心挑战是什么？
**参考答案：**

- 各集群资源水位不同
- 网络、地域、时延不同
- 调度决策要统一，但执行在不同集群
- 数据一致性和状态同步复杂
- 故障域扩大，排障更难

多集群不是简单“选一个集群扔过去”，而是要统一视图、统一策略、分布执行。

---

### 24. 多地域、多云调度时主要考虑什么？
**参考答案：**

主要考虑：

- 时延和网络成本
- 数据合规与数据就近
- 可用区容灾
- 成本差异
- 不同云厂商能力差异
- 控制面和数据面的一致性

---

### 25. 线上服务和离线训练混部时，最大的风险是什么？
**参考答案：**

风险主要是资源竞争导致在线服务抖动，比如：

- CPU 抢占
- 内存压力
- IO 抖动
- 网络带宽被训练流量打满
- 节点不稳定导致在线服务 SLA 下降

通常要通过：

- 资源隔离
- 优先级
- 节点池划分
- 限流
- 驱逐策略

来做保护。

---

### 26. 什么是 Failover？在训练系统里怎么理解？
**参考答案：**

Failover 就是在实例、节点、机房出问题时，把任务或流量切换到可用目标。  
训练系统里的 Failover 更复杂，因为训练任务通常有状态、耗时长、恢复代价大。  
所以常见做法是结合：

- checkpoint
- 重试策略
- 副本替换
- 节点级故障检测
- 跨集群迁移预案

---

### 27. 如何设计一个基础的训练任务可观测体系？
**参考答案：**

至少包含三层：

- **指标**：任务成功率、排队时长、调度时延、GPU 利用率、失败率、重试次数
- **日志**：调度日志、任务状态变更日志、容器日志
- **链路/事件**：任务生命周期事件、异常事件、关键状态流转

再往上是：

- 告警
- Dashboard
- 根因定位能力

---

### 28. 节点 NotReady 了，你怎么处理？
**参考答案：**

先区分是控制面感知问题，还是节点真实故障：

1. 看 Node 状态和事件
2. 看 kubelet 是否异常
3. 看网络、磁盘、内存压力
4. 看容器运行时是否异常
5. 决定是否 cordon / drain
6. 让工作负载迁移，必要时做节点替换

关键点：  
**排障和恢复要分开，先止损，再定位根因。**

---

### 29. Pod 一直 CrashLoopBackOff，怎么排查？
**参考答案：**

常规路径：

1. `kubectl logs` 看启动日志
2. `kubectl describe pod` 看事件
3. 检查探针是否配置错误
4. 检查配置文件、环境变量、依赖服务
5. 检查内存是否 OOM
6. 检查镜像版本、启动命令、权限问题

---

### 30. 如何理解 PDB（PodDisruptionBudget）？
**参考答案：**

PDB 用来限制“自愿中断”时能同时影响多少 Pod，比如升级、节点维护、drain。  
它可以保护服务可用性，避免一次性驱逐太多副本。  
基础设施面试里，PDB 常和 drain、eviction、滚动升级一起问。

---

## 五、K8s 扩展开发：Controller / Operator / CRD

### 31. 什么是 CRD？什么是 Operator？
**参考答案：**

- **CRD**：扩展 Kubernetes API，让你定义新的资源类型
- **Operator**：围绕某类资源写控制器，把领域知识编码进自动化运维逻辑

可以简洁地说：  
**CRD 定义对象，Operator 负责管理对象。**

---

### 32. Controller 的核心工作模式是什么？
**参考答案：**

核心就是 Reconcile Loop：

1. 监听资源变化
2. 拿到期望状态
3. 对比当前状态
4. 执行动作把系统拉回期望状态
5. 持续重复

这也是 K8s 声明式系统的核心。

---

### 33. Informer 的作用是什么？
**参考答案：**

Informer 用于高效监听和缓存资源对象变化。  
它通过本地缓存减少频繁访问 API Server 的成本，并通过事件通知驱动控制器工作。  
控制器开发中，Informer 几乎是基础设施。

---

### 34. Finalizer 是做什么的？
**参考答案：**

Finalizer 用于资源删除前的清理动作。  
当资源被删时，不会立即真正删除，而是先执行清理逻辑，比如：

- 释放外部资源
- 清理云资源
- 回收配额
- 写审计记录

清理完成后移除 Finalizer，资源才会彻底删除。

---

### 35. 如果让你设计一个“训练任务 Operator”，你会怎么做？
**参考答案：**

可以这样答：

1. 定义 `TrainingJob` CRD，描述镜像、资源、数据、副本数、优先级、重试策略
2. Controller 监听 `TrainingJob`
3. Reconcile 时创建对应 Pod/Service/ConfigMap
4. 管理任务状态流转：Pending、Running、Succeeded、Failed
5. 处理失败重试、超时、checkpoint、清理逻辑
6. 对接监控、事件、告警
7. 处理删除时的 Finalizer 清理

如果要更贴近 JD，可额外补：  
**支持多集群投递、gang scheduling、优先级和异构资源约束。**

---

## 六、Linux / Go / Python / 分布式基础

### 36. 容器本质是什么？
**参考答案：**

容器本质上不是虚拟机，而是 Linux 进程隔离与资源限制技术的组合，核心依赖：

- namespace：做隔离
- cgroups：做资源限制
- root filesystem：提供运行环境

---

### 37. 进程、线程、协程有什么区别？
**参考答案：**

- **进程**：资源分配基本单位，隔离性强
- **线程**：CPU 调度基本单位，共享进程资源
- **协程**：用户态轻量并发，由运行时调度，切换成本更低

如果岗位语言是 Go，可以补一句：  
Go 的 goroutine 就是一种用户态轻量并发实现。

---

### 38. Go 中 channel 和 mutex 分别适合什么场景？
**参考答案：**

- `channel` 更适合 goroutine 间通信、任务传递、流水线模型
- `mutex` 更适合保护共享状态

面试中不要绝对化地说谁更高级，关键是：  
**通信用 channel，共享内存保护用锁。**

---

### 39. context 在 Go 里有什么作用？
**参考答案：**

主要用于：

- 超时控制
- 取消传播
- 请求级上下文传递

在调度系统、控制器、RPC 调用链里都很常见。  
比如一个任务取消后，相关 goroutine 和下游调用都应该及时退出。

---

### 40. 你如何理解 CAP、Raft、分布式一致性？
**参考答案：**

简洁答法：

- **CAP**：分布式系统中一致性、可用性、分区容错性不能三者同时最大化
- **Raft**：一种更易理解的强一致性复制协议
- **一致性**：多个节点对同一数据最终看到什么结果、何时一致

K8s 里 etcd 就是典型的 Raft 应用。

---

## 七、系统设计题

### 41. 设计一个机器学习训练任务调度系统，你会怎么拆模块？
**参考答案：**

可以拆为：

- API 层：任务提交、查询、取消
- 资源视图层：统一汇总集群资源
- 调度层：优先级、资源匹配、拓扑感知、队列
- 执行层：对接 K8s/Yarn 等
- 状态管理层：任务状态机
- 稳定性层：重试、Failover、Checkpoint
- 观测层：监控、日志、告警、审计

---

### 42. 如果要提升 GPU 集群利用率，你会从哪些方面入手？
**参考答案：**

- 优化请求规格，减少过量申请
- 提升排队和回填策略
- 优化碎片化
- 做优先级和抢占
- 做训练/推理/在线混部治理
- 拓扑感知调度
- 优化失败重试和僵尸任务清理
- 做容量预测和弹性扩缩容

---

### 43. 如果要做“保障资源 + 超卖资源”体系，关键点是什么？
**参考答案：**

- 区分保底资源和可回收资源
- 定义抢占和回收规则
- 对在线业务设置保护阈值
- 建立优先级体系
- 对低优先级任务支持中断恢复
- 配套预算、审计和可观测能力

---

## 八、高频排障题

### 44. Service 不通，你怎么查？
**参考答案：**

1. Pod 是否 Ready
2. Service selector 是否匹配 Pod
3. Endpoints 是否生成
4. Pod 监听端口是否正确
5. kube-proxy 规则是否正常
6. 网络策略是否拦截
7. DNS 解析是否正常

---

### 45. CoreDNS 异常会导致什么现象？怎么排查？
**参考答案：**

常见现象：

- 服务名无法解析
- 应用启动超时
- 跨服务调用失败

排查重点：

- CoreDNS Pod 状态
- 配置文件 Corefile
- 上游 DNS 可达性
- 集群 DNS Service 是否正常
- 网络插件是否异常

---

### 46. 节点资源明明看起来有空闲，但 Pod 调度不上去，为什么？
**参考答案：**

因为调度器看的是**可分配资源和约束条件**，不是简单看机器实时监控。  
可能原因：

- requests 已经占满
- GPU 设备不足
- 污点/亲和性限制
- 拓扑不满足
- PVC 未绑定
- 可分配资源与监控口径不同

---

## 九、项目经验题（你一定要提前准备）

### 47. 你做过和 K8s/调度最相关的项目是什么？
**参考答案模板：**

按这个结构回答：

- **背景**：遇到了什么问题
- **目标**：要优化什么指标
- **方案**：你设计了哪些模块
- **难点**：调度、稳定性、性能、兼容性
- **你的贡献**：你具体负责什么
- **结果**：指标提升多少
- **复盘**：还有什么可以继续优化

---

### 48. 如果你没有真正做过大规模调度项目，怎么回答更稳？
**参考答案：**

不要硬编规模，可以这么说：

- 我在项目中接触过 K8s 部署、任务编排、资源配置、故障排查
- 对调度器、抢占、扩缩容、Operator 的原理做过系统学习
- 如果让我做更复杂的资源联合调度，我会先从任务建模、资源抽象、优先级策略和可观测体系入手

面试官更看重：  
**你是否理解问题、是否有工程思维、是否能快速学习。**

---

## 十、这份 JD 下最该重点背熟的 12 题

如果时间有限，优先准备下面这 12 个：

1. Pod 创建流程  
2. Deployment / StatefulSet / DaemonSet 区别  
3. Service 与 kube-proxy 原理  
4. 调度器 Filter / Score 机制  
5. requests / limits / QoS  
6. Pod Pending 排查  
7. 优先级、抢占、驱逐  
8. HPA / VPA / Cluster Autoscaler  
9. GPU 调度和拓扑感知调度  
10. Gang Scheduling 在训练场景中的价值  
11. CRD / Controller / Operator / Reconcile  
12. 训练任务 Failover、Checkpoint、可观测性设计  

---

## 十一、面试回答技巧

### 推荐回答结构
用这套模板最稳：

**结论 → 原理 → 场景 → 排障/优化**

例如回答“Pod Pending 怎么查”：

- **结论**：先看 Events，通常是调度条件不满足
- **原理**：调度器需要同时满足资源、约束、存储等条件
- **场景**：常见是资源不足、污点、亲和性冲突、PVC 未绑定
- **排障/优化**：describe Pod、检查 Node、PVC、调度器日志，再优化资源申请和调度策略

---

## 十二、最后建议：如何把这份清单用好

建议你分三轮准备：

### 第一轮：背概念
把每题先讲到 1 分钟能说清

### 第二轮：连到项目
每个知识点都想一下：  
“我项目里哪里能对应上？”

### 第三轮：做模拟
找同学或自己录音，练成：
- 基础题：1 分钟
- 原理题：3 分钟
- 设计题：5 分钟

---

## 十三、适合你继续深入的加分点

如果你想冲更强表现，建议继续补：

- `client-go` / `controller-runtime`
- K8s Scheduler Framework
- Volcano / Kueue / YuniKorn 这类批调度框架
- GPU Operator / device plugin
- Linux cgroups / namespace
- Prometheus 指标体系
- Go 并发与 context

